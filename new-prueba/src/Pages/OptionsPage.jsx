import React, {useState} from 'react'
import Options from "../Components/Options";

const OprtionsPage = ({handleAddForm}) => {
return(
    <Options handleAddForm={handleAddForm}/>
)
}
export default OprtionsPage
